name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        pip install pylint mypy pytest pytest-cov

    - name: Run pylint
      run: pylint UNEZPAnOL.py || true

    - name: Run mypy
      run: mypy UNEZPAnOL.py || true
    - name: Run tests
    Prueba de ejecución del pipeline.
      run: pytest --cov=UNEZPAnOL || true
